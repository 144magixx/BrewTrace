# 前端说明

当前前端实现为离线可验证的 TypeScript 工作台组件模型，目录和交互契约按 React + Vite 工作台设计组织。依赖仓库恢复后可接入真实 `vite`、`react`、`@testing-library/react` 和 Playwright。

## 命令

```bash
npm test
npm run build
npm run test:e2e
npm run dev
```
